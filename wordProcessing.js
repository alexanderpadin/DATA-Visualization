/*
*	Word procesing script
*
*
*/
function word_processing(str) { //str=[]

	//Delete alphanumeric 
	function clean(arg) {
		var str = arg
		return str
	}

	//Remove prepositions adn article words
	function remove_preposition(arg) {
		var str = arg
		return str
	}

	function generate_root(arg) {
		var str = arg
	}

}